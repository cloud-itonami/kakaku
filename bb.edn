{:paths ["src" "test"] :deps {
        ;; kotoba.bytes.sha256 replaced java.security.MessageDigest here (2026-09-24).
        ;; An explicit floor: tools.deps takes the NEWEST sha it is shown.
        io.github.kotoba-lang/bytes {:git/sha "e28a3bb010f240451dd8be22b087c920633ec28d"}
        ;; kotoba.io replaced clojure.java.io here (2026-09-24). An explicit floor:
        ;; kotoba.io carries clojure.java.io's file/as-file/as-relative-path/
        ;; make-parents/delete-file from 98045c7, and tools.deps takes the NEWEST
        ;; sha it is shown.
        io.github.kotoba-lang/io {:git/sha "f7b3fca8e08147ae7d658fe36ae0475a45fb76a9"}
        ;; kotoba.process replaced clojure.java.shell here (2026-09-24). An explicit
        ;; floor: kotoba.process/sh parses clojure.java.shell's options from db49ea9,
        ;; and tools.deps takes the NEWEST sha it is shown.
        io.github.kotoba-lang/process {:git/sha "db49ea936e53d0bc698be0db79bf96ad86b5f7d0"}
        ;; kotoba.lang.edn replaced clojure.edn here (2026-09-23). An explicit floor:
        ;; tools.deps takes the NEWEST sha it is shown, and kbb resolves git deps
        ;; only from nbb.edn, so every project file this repo has names it.
        io.github.kotoba-lang/edn {:git/sha "79dba32c8892058842c57783f06a2dfe4601aeb0"}
       cheshire/cheshire {:mvn/version "5.13.0"}}
 :tasks {test {:requires ([clojure.test] [kakaku.kotoba.test-ingest-mcp]
                          [kakaku.methods.test-agent] [kakaku.methods.test-autorun]
                          [kakaku.methods.test-ingest] [kakaku.methods.test-kakaku-edn]
                          [kakaku.methods.test-pipeline-cid] [kakaku.murakumo-test]
                          [kakaku.viz.test-build-viz])
               :task (let [r (clojure.test/run-tests 'kakaku.kotoba.test-ingest-mcp
                              'kakaku.methods.test-agent 'kakaku.methods.test-autorun
                              'kakaku.methods.test-ingest 'kakaku.methods.test-kakaku-edn
                              'kakaku.methods.test-pipeline-cid 'kakaku.murakumo-test
                              'kakaku.viz.test-build-viz)]
                       (when (pos? (+ (:fail r) (:error r))) (throw (ex-info "kakaku tests failed" r))) r)}}}
